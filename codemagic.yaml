workflows:
  default-workflow:
    name: Laravel Build Workflow
    environment:
      node: latest
    scripts:
      - name: Install dependencies
        script: |
          composer install
          npm install
      - name: Run tests
        script: |
          php artisan test
